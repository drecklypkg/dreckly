$NetBSD$

Support treating Darwin like any other Unix.

--- toolkit/moz.configure.orig	2025-03-14 13:13:48.902275498 +0000
+++ toolkit/moz.configure
@@ -472,7 +472,7 @@ def toolkit_choices(target):
     if target.os == "WINNT":
         return ("cairo-windows",)
     elif target.os == "OSX":
-        return ("cairo-cocoa",)
+        return ("cairo-cocoa","cairo-gtk3")
     elif target.os == "iOS":
         return ("cairo-uikit",)
     elif target.os == "Android":
