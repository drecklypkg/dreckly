$NetBSD$

Honour environment variables from infrastructure.

Make sure we link with CXX.

--- Makefile.orig	2012-06-04 22:33:07.000000000 +0000
+++ Makefile
@@ -5,9 +5,8 @@ OPT =
 WARN = -Wall -W -g
 #OPT = -O2
 OPT =
-CPPFLAGS = $(ADDITIONAL_CPPFLAGS)
-CXXFLAGS = $(WARN) $(OPT)
-CXX = g++
+CPPFLAGS += $(ADDITIONAL_CPPFLAGS)
+CXXFLAGS += $(WARN) $(OPT)
 CC = $(CXX)
 
 # --- default target
